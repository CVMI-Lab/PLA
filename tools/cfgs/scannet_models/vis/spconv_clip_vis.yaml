_BASE_CONFIG_: cfgs/scannet_models/spconv_clip.yaml

CLASS_NAMES: [kitchen]

DATA_CONFIG:
  ignore_class_idx: []


MODEL:
  TASK_HEAD:
    TEXT_EMBED:
      PATH: 'None'

TEXT_ENCODER:
  NAME: CLIP
  BACKBONE: ViT-B/16  # ['RN50', 'RN101', 'RN50x4', 'RN50x16', 'RN50x64', 'ViT-B/32', 'ViT-B/16', 'ViT-L/14']
  TEMPLATE: identity
  EXTRACT_EMBED: True  # Online extract text embeding from class or not
  CATEGORY_NAMES: [kitchen]
#  BERT:
#  BACKBONE: bert-base-uncased
